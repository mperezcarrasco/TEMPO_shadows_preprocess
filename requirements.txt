# DARCLOS-TEMPO pipeline dependencies (exact pins for reproducibility).
#
# Regenerate from a working environment with:
#     pip freeze | grep -Ei '^(numpy|scipy|h5py|netCDF4|rasterio|fiona|shapely|geopandas|PyYAML)=='
#
# When bumping a pin, run scripts/run_scan.py on the September 2025 corpus
# and confirm the outputs still pass visual QA before committing the change.

# --- Numerics ---
numpy==2.1.3
scipy==1.14.1

# --- HDF5-backed NetCDF readers (used for L1B and L2 access) ---
h5py==3.12.1
netCDF4==1.7.2

# --- Geospatial output (GeoTIFF + shapefile) ---
rasterio==1.4.3
fiona==1.10.1
shapely==2.0.6
geopandas==1.0.1

# --- Config ---
PyYAML==6.0.2
